[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npx serve -s dist -l $PORT"
healthcheckPath = "/"
healthcheckTimeout = 60

[build.nixpacksPlan.phases.build]
cmds = ["npm install", "npm run build", "npm install -g serve"]
